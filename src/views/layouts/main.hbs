<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>InnovaTube</title>

  <!-- Tailwind -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- FontAwesome -->
  <link href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css" rel="stylesheet">

  <style>
    body {
      background-color: #f3f4f6;
      font-family: 'Inter', sans-serif;
    }

    /* 🔹 Navbar compacta */
    nav {
      background-color: #2b9150; /* Verde */
      color: white;
      padding: 0.6rem 2rem;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
    }

    nav h1 {
      font-size: 1.3rem;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    nav ul {
      display: flex;
      align-items: center;
      gap: 1.2rem;
    }

    nav a {
      color: white;
      font-weight: 500;
      transition: color 0.2s ease-in-out;
    }

    nav a:hover {
      color: #d1fae5; /* Verde claro al pasar el ratón */
    }

    .btn-logout {
      background-color: #dc2626;
      color: white;
      padding: 0.3rem 0.9rem;
      border-radius: 6px;
      font-weight: 600;
      transition: background 0.2s ease-in-out;
    }

    .btn-logout:hover {
      background-color: #b91c1c;
    }

    .btn-register {
      border: 1px solid white;
      padding: 0.3rem 0.9rem;
      border-radius: 6px;
      transition: all 0.2s ease-in-out;
    }

    .btn-register:hover {
      background-color: white;
      color: #16a34a;
    }
  </style>
</head>

<body class="bg-gray-100">

  <!-- 🔹 Navbar compacta -->
  <nav class="flex items-center justify-between">
    <div class="flex items-center space-x-2">
      <i class="fas fa-play-circle text-xl"></i>
      <h1>InnovaTube</h1>
    </div>

    <ul>
      {{#if name}}
        <li class="text-sm font-semibold opacity-90">Bienvenido, {{name}}</li>
        {{!-- <li><a href="/home" class="hover:underline">Inicio</a></li> --}}
        <li><a href="/favoritos" class="text-sm font-semibold opacity-90 hover:underline">Favoritos</a></li>
        <li><a href="/logout" class="btn-logout">Cerrar sesión</a></li>
      {{else}}
        <li><a href="/login" class="hover:underline">Iniciar sesión</a></li>
        <li><a href="/register" class="btn-register">Registrarse</a></li>
      {{/if}}
    </ul>
  </nav>

  <!-- 🔹 Contenido -->
  <main class="p-6 max-w-7xl mx-auto">
    {{{body}}}
  </main>

</body>
</html>
